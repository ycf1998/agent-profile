{
  "name": "@money1998/agent-profile",
  "version": "0.2.0",
  "description": "Local agent profile asset manager",
  "license": "MIT",
  "keywords": [
    "agent",
    "profile",
    "cli",
    "configuration",
    "developer-tools"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:ycf1998/agent-profile.git"
  },
  "homepage": "https://github.com/ycf1998/agent-profile",
  "bugs": {
    "url": "https://github.com/ycf1998/agent-profile/issues"
  },
  "bin": {
    "agent-profile": "dist/cli.js"
  },
  "files": [
    "dist/cli.js",
    "dist/commands",
    "dist/core",
    "dist/models",
    "dist/utils",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{ recursive: true, force: true })\" && tsc -p tsconfig.json",
    "test": "npm run build && node dist/tests/integration.js",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
